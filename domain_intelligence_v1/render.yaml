services:
  - type: web
    name: domain-intelligence-v1
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DOMAIN_API_KEY
        generateValue: true
